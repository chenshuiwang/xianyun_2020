export const state = () => ({
    draft: [{title:'12132'}]
})
// export const mutations = {
//     setDraft(state, data) {
//         state.draft.unshift(data);
//         if (state.draft.length > 5) {
//             state.draft.length = 5;
//         }
//     }
// }